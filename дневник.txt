19-03-2020
----------
Когда сделал :
Array.Copy(magLog, MAG_LOG, magLog.Length);
Array.Copy(t, TSAMPL, t.Length);

и массивы-приёмники были по 64000 элементов то прога сильно лагала, а на 4096 - норм.

Добавил:
 if (this.Disposed) return;
в THREAD - чтобы при выключении не было эксцепшенов.
 